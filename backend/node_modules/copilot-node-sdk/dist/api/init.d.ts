import { DefaultService as Copilot } from '../codegen/api';
interface Token {
  clientId?: string;
  companyId?: string;
  internalUserId?: string;
  workspaceId: string;
  notificationId?: string;
}
export type CopilotAPI = typeof Copilot & {
  getTokenPayload?: () => Promise<Token>;
};
export declare function copilotApi({
  apiKey,
  token: tokenString,
}: {
  apiKey: string;
  token?: string;
}): CopilotAPI;
export {};
